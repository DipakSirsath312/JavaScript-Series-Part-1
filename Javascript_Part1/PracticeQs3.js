// Practice Qs.

// Qs. Declare your name as string and print its length in JS.
let str = "Dipak Shirsath";
console.log(str.length)

// Qs. Decalre your first name as a string and print it's first character
console.log(str[0]);

// Qs. Declare your first name as a string and print its last character.
console.log(str[str.length-10]);

// Qs What is output of following code :

// "Madara just said" + "💀" + 106
console.log("Madara just said"+"💀"+ 106);

// Qs What are lengths of an empty string & a string with a single space?.
let str2 = ""; // empty
let str3 = " "; // space

console.log(str2.length);
console.log(str3.length);